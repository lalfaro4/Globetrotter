const sendButton = document.getElementById("send-button");

// When the user clicks the "Send" button on the "Reset Password" page, a alert will be displayed to the user.
sendButton.addEventListener("click", () => {
    alert("Email to resest password has been sent to the provided email!")
});
